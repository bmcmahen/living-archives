.swipe-content.white.top.right.one-thirds.animated
	h3=locals.title
	p.subhead=locals.location
	p !{locals.fullDescription}
	if locals.resources
		for resource in resources
			p.small !{resource.resource}
	if locals.image
		.images
